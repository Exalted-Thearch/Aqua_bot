
// const nexus_coin_emoji = "<:nexus_bronze_coin:1396438179288191007>"; 


// function formatCurrency(amount) {
//     if (typeof amount !== 'number' || isNaN(amount)) {
//         // Handle cases where amount might not be a valid number
//         return `${nexus_coin_emoji} ---`; // Or throw an error, depending on desired behavior
//     }
//     return `${nexus_coin_emoji} ${amount.toLocaleString()}`;
// }

// function formatChange(amount) {
//     if (typeof amount !== 'number' || isNaN(amount)) {
//         return `---`;
//     }
//     return `${amount > 0 ? '+' : ''}${amount.toLocaleString()}`;
// }

// module.exports = { formatCurrency, formatChange };